# Supabase config for Oura OAuth Edge Function only.
# Run Supabase CLI commands from the supabase-oura directory.

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[functions]
verify_jwt = false
